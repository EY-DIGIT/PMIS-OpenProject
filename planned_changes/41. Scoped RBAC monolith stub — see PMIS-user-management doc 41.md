# Doc 41 (monolith stub) — Scoped RBAC

**Canonical doc lives in PMIS-user-management/planned_changes/41.**
This file is a stub so monolith-side reviewers can find the doc by
number when grepping `git log` or browsing the folder.

## What landed in monolith for doc 41

| File | Change |
|---|---|
| `app/infrastructure/db/models/user_role_assignment.py` | New mirror model (canonical owner = user-management). |
| `app/infrastructure/db/models/__init__.py` | Register the new model. |
| `alembic/versions/a1b2c3d4e5f6_doc41_user_role_assignments.py` | Create table + idempotent backfill from `user_roles` and `project_members.roles[]`. |
| `app/core/permissions.py` | Add `USERS_GRANT_SUPERADMIN` permission code + 5 scoped role names (`super_admin` / `org_admin` / `project_admin` / `project_member` / `division_member`). |
| `app/core/middleware/auth.py` | Hydrate `request.state.scoped_permissions` (`Dict[(scope_kind, scope_id), Set[str]]`) on each authenticated request. |
| `app/core/middleware/rbac.py` | New `require_project_permission(code)` and `require_org_permission(code)` helpers with M-A-T-S ancestor resolver chain. |
| `app/api/v3/projects/routes.py` | Switch PATCH/DELETE/save/publish/close to `require_project_permission`. |
| `app/api/v3/milestones/routes.py` | Switch create/update/delete/restore writes. |
| `app/api/v3/activities/routes.py` | Switch create/update/delete/restore writes. |
| `app/api/v3/tasks/routes.py` | Switch create/update/delete/restore writes. |
| `app/api/v3/subtasks/routes.py` | Switch create/update/delete/restore writes. |
| `app/api/v3/project_members/routes.py` | Switch add/update/delete writes. |
| `app/infrastructure/db/repositories/rbac_repository.py` | Extend with `effective_permissions_by_scope` + scoped admin lookup. |

## What did NOT land in monolith

- Comment / attachment write routes — left on the legacy union path.
  See the canonical doc's "Monolith touch surface" → "Routes
  intentionally LEFT on `require_permission`" for rationale.
- The role-assignment CRUD + project-mapping view endpoints — those
  are owned by user-management.
