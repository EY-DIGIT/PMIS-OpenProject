# Doc 42 (monolith stub) — Post-doc-41 polish

**Canonical doc lives at
[`PMIS-user-management/planned_changes/4. Post-doc-3 polish …`](../../PMIS-user-management/planned_changes/).**
> The canonical doc used to be at user-mgmt `42.` and was renumbered
> to `4.` on 2026-05-12 when the project switched to per-repo local
> doc numbering. This monolith stub keeps its `42.` number.

This file is a stub so monolith reviewers can find the doc by number
when grepping `git log` or browsing the folder.

## Monolith-side changes that landed under doc 42

| Commit | What |
|---|---|
| `361ac98` | Trim `PROJECT_ADMIN_ROLE_PERMISSIONS` (drop `MILESTONES_UPDATE` + `ACTIVITIES_UPDATE`). Mirror of user-mgmt `38c74e6`. |
| `e08333d` | Router include order tidied — legacy / superseded routers (project_members, roles, permissions, work_packages*, meetings*) moved to the bottom of the include list so Swagger UI puts active surfaces first. master_data position preserved. Also locks the proxy surface: doc-41 project- and vendor-side endpoints are intentionally NOT forwarded through `UserServiceProxyMiddleware` (FE talks to user-mgmt :8001 directly for those). |
| `a6edfe7` | Consolidated docs touch-up: `RBAC_GUIDE.md` doc-41 endpoint table now annotates proxy reach per row; `DEPLOYMENT_AND_INTEGRATION.md` microservice-extraction note same. Removed the doc-41 flows stub (canonical now at `C:\Programming\PMIS\DOC_41_FLOWS.md`, outside both repos). |

## What did NOT land in monolith

- The notification-service URL fix (#1 in canonical) — config-only,
  applied directly on the deploy host's `.env`.
- The `CORSMiddleware` reorder (#3) — user-mgmt only; monolith was
  already correct (doc 39).
- Deprecation markers on `/users/{id}/roles*` (#4) — user-mgmt owns
  those handlers.

Live state after doc 42: `pmic-backend:v85` running on the deploy
host. Rollback target: `:v82` (pre-doc-41).
