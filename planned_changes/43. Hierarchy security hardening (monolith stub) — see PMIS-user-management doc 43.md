# Doc 43 (monolith stub) — Hierarchy security hardening

**Canonical doc lives in
[`PMIS-user-management/planned_changes/43`](../../PMIS-user-management/planned_changes/).**
This file is a stub so monolith reviewers can find the doc by number.

## Monolith-side changes that landed under doc 43

| File | Change |
|---|---|
| `app/core/permissions.py` | Already had `ADMIN_FULL_ROLE_PERMISSIONS` (excluding `users:grant_superadmin`) — now actually used. |
| `app/core/config.py` | New `BOOTSTRAP_SUPERADMIN_LOGIN` / `_EMAIL` / `_PASSWORD` settings (defaults `super_admin` / `super_admin@example.com` / `superadmin123`). |
| `app/infrastructure/db/repositories/rbac_repository.py` | admin role seeded with `ADMIN_FULL_ROLE_PERMISSIONS` instead of `ADMIN_ROLE_PERMISSIONS`. Boot-time self-heal revoke of `users:grant_superadmin` from admin. Role descriptions rewritten without internal doc-number references. super_admin role row gets the same lock-from-mutation as admin. |
| `app/infrastructure/db/session.py` | Bootstrap admin user creation REMOVED. Bootstrap super_admin user creation ADDED — uses doc-41 `user_role_assignments` table for the global super_admin grant. |
| `app/main.py` | Universal-OTP startup warning when `UNIVERSAL_OTP_ENABLED=true`. |

## What did NOT land in monolith

- F1 caller-vs-target hierarchy gate on user-mutation routes — those
  routes live in user-mgmt; monolith proxies through.
- F2 reserved-permission guards — same, user-mgmt-only.
- F3 last-super_admin lockout on user-DELETE — same, user-mgmt-only.
- Round-2 hardening (G1 self-deactivate; G2 peer SA password-change;
  G3 peer SA DELETE) — same, user-mgmt service is the source of
  truth for user-mutation guards. Monolith proxy doesn't need changes.

Monolith full test suite: 626 passed, 3 skipped (no test changes
needed beyond the role-description string updates that the existing
`test_admin_sees_full_set_minus_grant_superadmin` already covers in
the prior doc-42 run).

Live: `pmic-backend:v86` running on the deploy host. Rollback target:
`v85` (post-doc-42), `v82` (pre-doc-41).
